@model IEnumerable<L01P02_2020ML603_BLOG.Models.roles>

@{
    ViewData["Title"] = "Index";
}

<h1>Administrador de Roles</h1>

<p>
    <a class="btn btn-outline-primary" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.rol)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.rol)
            </td>
            <td>
                    <a class="btn btn-success" asp-action="Edit" asp-route-id="@item.rolId">Edit</a> |
                    <a class="btn btn-warning" asp-action="Details" asp-route-id="@item.rolId">Details</a> |
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.rolId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
